<h1>Create New Song Form</h1>

<%= form_for(@song) do |f| %>
    <label>Song Name</label>
    <%= f.text_field :name %><br>

    <label>Genre</label>
    <%= f.text_field :genre_id %><br>

    <label>Artist</label>
    <%= f.text_field :artist_id %>

    <%= f.submit "Create Song" %>
    <% end %>